---
# defaults file for cprestart
kube_manifests_dir: /etc/kubernetes/manifests
tmp_base_dir: /tmp/kubernetes
kube_manifests_backup_dir: '{{ tmp_base_dir}}/manifests'
etcdctl_sh : /usr/local/bin/etcdctl.sh
cri_reference: '{{ tmp_base_dir }}/cri.txt'
retry_count: 30
retry_delay: 5
k8s_healthy: false