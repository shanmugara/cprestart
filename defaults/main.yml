---
# defaults file for cprestart
kube_manifests_dir: /etc/kubernetes/manifests
kube_manifests_backup_dir: /tmp/kubernetes/manifests
etcdctl_sh : /usr/local/bin/etcdctl.sh
retry_count: 30
retry_delay: 5